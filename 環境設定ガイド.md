# ZeroTraceBrowser 環境設定ガイド

## 1. プロジェクト概要

ZeroTraceBrowser は、Python + FastAPI で構成されたローカル画像ブラウザ兼、明示的に制御されたファイル操作ツールです。

ローカル Web サービスとして画面を提供し、次の用途に使います。

- ローカル画像ディレクトリの閲覧
- 画像と基本メタデータの確認
- サムネイルキャッシュの生成
- タイムラインによる並び替えとグルーピング
- 画像のコピー
- 画像をアプリ内リサイクル領域へ移動
- Hash DB と重複画像検出結果の生成
- アプリ内リサイクル領域と実行時データの管理

本プロジェクトはローカル利用を前提としており、データベース、クラウドサービス、外部アカウントは不要です。

## 2. 推奨環境

- OS: Windows 10 または Windows 11
- Python: 3.10 以上
- ターミナル: PowerShell
- ブラウザ: Microsoft Edge、Chrome、Firefox などのモダンブラウザ

プロジェクトルート配下に repo-local の仮想環境を作成することを推奨します。

```text
.\venv\
```

これにより、依存関係がシステム Python や別プロジェクトの仮想環境にインストールされることを避けられます。

## 3. Python 依存関係

依存関係は `requirements.txt` で管理されています。現在の主な依存関係は次のとおりです。

- `fastapi`
- `uvicorn[standard]`
- `Pillow`
- `exifread`
- `pywin32`（Windows）
- `pytest`
- `httpx`

用途：

- `FastAPI`: バックエンド Web API
- `uvicorn`: ローカル Web サービスの実行
- `Pillow`: 画像読み込み、サムネイル生成、一部メタデータ処理
- `exifread`: EXIF メタデータの読み取り
- `pywin32`: Windows でのファイル時刻保持などのシステムファイル操作
- `pytest`: 自動テスト
- `httpx`: テストクライアント依存関係

## 4. 仮想環境の作成

プロジェクトルートで PowerShell を開きます。

```powershell
cd D:\path\to\ZeroTraceBrowser
```

仮想環境を作成します。

```powershell
python -m venv venv
```

作成後、プロジェクトディレクトリに次のフォルダができます。

```text
venv\
```

よく使うパス：

```text
.\venv\Scripts\python.exe
.\venv\Scripts\pip.exe
```

仮想環境を有効化したい場合：

```powershell
.\venv\Scripts\Activate.ps1
```

PowerShell がスクリプト実行をブロックする場合は、現在のターミナルセッションで一時的に許可できます。

```powershell
Set-ExecutionPolicy -Scope Process RemoteSigned
```

ただし、本プロジェクトでは `.\venv\Scripts\python.exe` を直接使う方法を推奨します。仮想環境を有効化していなくても、プロジェクト専用の Python を使っていることが明確になります。

## 5. 依存関係のインストール

明示的な Python パスを使ってインストールします。

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

任意で、pip を更新してからインストールします。

```powershell
.\venv\Scripts\python.exe -m pip install --upgrade pip
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

主要な依存関係を確認します。

```powershell
.\venv\Scripts\python.exe -c "import fastapi, uvicorn; from PIL import Image; print('Environment OK')"
```

EXIF と Windows ファイル操作関連の依存関係を個別に確認する場合：

```powershell
.\venv\Scripts\python.exe -c "import exifread; print('exifread OK')"
.\venv\Scripts\python.exe -c "import win32file, win32con; print('pywin32 OK')"
```

`pywin32` は Windows 環境でのみ必要です。本プロジェクトは Windows 10 / Windows 11 での利用を推奨します。

## 6. サーバーの起動

プロジェクト付属のスクリプトを使う方法を推奨します。

```powershell
.\start.ps1
```

このスクリプトは、プロジェクトルートから次のコマンドでサーバーを起動します。

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```

手動で実行することもできます。

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```

起動に成功すると、通常はターミナルに次のような表示が出ます。

```text
Uvicorn running on http://127.0.0.1:8000
```

その後、ブラウザで次の URL を開きます。

```text
http://127.0.0.1:8000
```

## 7. ポート

デフォルトのポートは次のとおりです。

```text
8000
```

すでに使用中の場合は、一時的に別ポートで起動できます。

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8001
```

その場合は次を開きます。

```text
http://127.0.0.1:8001
```

注意: `start.ps1` は現在 `8000` ポートを使用します。

## 8. テストの実行

プロジェクト付属のスクリプトを使う方法を推奨します。

```powershell
.\test.ps1
```

このスクリプトは次を実行します。

```powershell
.\venv\Scripts\python.exe -m pytest -q
```

pytest を直接実行することもできます。

```powershell
.\venv\Scripts\python.exe -m pytest -q
```

## 9. 主なファイルとディレクトリ

```text
ZeroTraceBrowser/
├── app.py                      # FastAPI アプリケーション入口
├── requirements.txt            # Python 依存関係
├── requirements-dev.txt        # テスト依存関係
├── start.ps1                   # 起動スクリプト
├── test.ps1                    # テストスクリプト
├── pyproject.toml              # プロジェクトメタデータ

├── core/                       # バックエンド中核
│   ├── app/                    # アプリケーションファクトリ、ライフサイクル、ミドルウェア
│   ├── config/                 # パス、拡張子、対応フォーマット
│   ├── context_modules/        # コンテキストモジュール（ドメイン別に分割）
│   ├── domain/                 # ドメインモデル：RootContext、ImageEntry など
│   ├── infrastructure/         # ファイル転送、ハッシュ計算、画像処理
│   ├── repositories/           # データアクセス層
│   ├── routes/                 # API ルートハンドラ
│   ├── services/               # ビジネスサービス
│   └── use_cases/              # ユースケース層（コピー、削除、復元など）
├── MediaArchiveOrganizer/      # 画像解析・整理関連モジュール
├── tests/                      # 自動テスト
├── data/                       # 実行時データ（画像ルートごとに分離）
│   └── roots/<root_id>/
│       ├── root.json           # ルート設定
│       ├── deleted/            # アプリ内リサイクル領域
│       ├── thumbnails/         # サムネイルキャッシュ
│       ├── logs/               # 操作ログ（CSV 形式）
│       ├── indexes/            # 画像インデックス / タイムラインインデックス
│       ├── tasks/              # タスク出力（タスク別）
│       ├── hash_db.sqlite3     # コンテンツハッシュデータベース
│       └── duplicates.json     # 重複画像検出結果
```

主要な実行時データは、画像ルートごとに次の場所へ分離されます。

```text
data/roots/<root_id>/
```

以下が含まれる場合があります。

- `deleted/`: アプリ内リサイクル領域
- `thumbnails/`: サムネイルキャッシュ
- `logs/`: 操作ログ
- `indexes/`: 画像インデックスとタイムラインインデックス
- `tasks/`: タスク出力
- `duplicates.json`: 重複画像結果
- `hash_db.sqlite3`: Hash DB

## 10. ローカル設定ファイル

プロジェクト設定は次に保存されます。

```text
settings.json
```

主な設定：

- 現在の画像ルート
- 追加済み画像ルート一覧
- デフォルトのコピー先ディレクトリ
- 言語設定
- タスクページのデフォルト値
- 各画像ルートの統計サマリー

設定変更は Settings ページから行うことを推奨します。形式を理解していない場合は、`settings.json` を手動編集しないでください。

## 11. よくある環境トラブル

### Python が認識されない

PowerShell で `python` が見つからない場合は、以下を確認してください。

- Python がインストールされていること
- Python がシステムの `PATH` に追加されていること
- または Python Launcher を使うこと: `py -m venv venv`

### 依存関係が別の環境にインストールされた

常に次のコマンドを優先してください。

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

`pip install ...` だけに依存しないでください。システム Python や別の環境を指している可能性があります。

現在使っている Python を確認します。

```powershell
.\venv\Scripts\python.exe -c "import sys; print(sys.executable)"
```

### Pillow / exifread / pywin32 が見つからない、または import に失敗する

`PIL`、`exifread`、`win32file`、`win32con` に関するエラーが出る場合は、依存関係を再インストールしてください。

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

現在の仮想環境にパッケージが入っているか確認することもできます。

```powershell
.\venv\Scripts\python.exe -m pip show Pillow
.\venv\Scripts\python.exe -m pip show exifread
.\venv\Scripts\python.exe -m pip show pywin32
```

### PowerShell でスクリプトを実行できない

`Activate.ps1`、`start.ps1`、`test.ps1` が実行ポリシーでブロックされる場合は、現在のターミナルセッションで次を実行してください。

```powershell
Set-ExecutionPolicy -Scope Process RemoteSigned
```

その後、スクリプトを再実行します。

### ポート 8000 が使用中

ポート使用中で起動できない場合：

1. すでに起動している ZeroTraceBrowser サービスを停止する
2. または、別ポートで手動起動する

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8001
```

### 変更後もページが古いままに見える

フロントエンド JS、CSS、バックエンドコードを変更したのに古い挙動が表示される場合：

1. Uvicorn サービスを再起動する
2. ブラウザを更新する
3. 必要に応じてハードリロードする

バックエンドコードの変更は、通常サーバー再起動が必要です。

### 画像が表示されない、またはサムネイルが遅い

大きな画像ディレクトリを初めて開く場合、サムネイル生成が必要になるため、初回表示が遅くなることがあります。以降は通常キャッシュが利用されます。

画像が表示されない場合は、以下を確認してください。

- 画像ルートが存在すること
- 現在のユーザーに読み取り権限があること
- ファイル形式が対応していること
- サーバー側ターミナルに関連エラーが出ていないこと

## 12. 初回設定の推奨手順

1. プロジェクトルートで PowerShell を開く
2. 仮想環境を作成する

   ```powershell
   python -m venv venv
   ```

3. 依存関係をインストールする

   ```powershell
   .\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
   ```

4. サーバーを起動する

   ```powershell
   .\start.ps1
   ```

5. ブラウザを開く

   ```text
   http://127.0.0.1:8000
   ```

6. Settings ページで画像ルートを追加する
7. 必要に応じてデフォルトのコピー先ディレクトリを設定する
8. Gallery ページに戻って閲覧を開始する
9. プロジェクト状態を確認したい場合は、次を実行する

   ```powershell
   .\test.ps1
   ```

## 13. 安全上の推奨事項

- 現在のバージョンは、本機または信頼できる LAN 環境でのみ使用してください
- サービスを公開インターネットへ直接公開しないでください
- 重要な画像ディレクトリに対して一括操作を行う前に、必ずバックアップしてください
- ファイル削除後は、Recycle ページで復元できるか確認してください
- アプリ内リサイクル領域をクリーンアップする前に、ファイルが不要であることを確認してください
- `data/roots/<root_id>/` 以下の実行時データを不用意に削除しないでください

## 14. クイックコマンド

```powershell
python -m venv venv
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
.\start.ps1
```

テスト：

```powershell
.\test.ps1
```

手動起動：

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```
