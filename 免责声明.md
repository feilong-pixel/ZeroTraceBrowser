# ZeroTraceBrowser 免责声明

## 1. 使用风险说明

ZeroTraceBrowser 是一个本地图片浏览与受控文件操作工具。

本工具不会自动替用户整理、删除或移动文件。复制、删除、批量处理、回收区清理、目录历史数据清理等操作均需要用户明确触发。

尽管本工具尽量以可预期、可恢复的方式处理文件，但在实际使用过程中，仍可能因文件权限、路径设置错误、磁盘状态、文件被占用、异常中断、系统限制、第三方库行为或其他不可预见原因导致以下情况：

- 文件操作结果不符合预期
- 文件复制失败或复制到非预期目录
- 文件被移动到应用内回收区后无法按预期恢复
- 缩略图、索引、日志或任务结果不完整
- 重复检测结果不准确或不完整
- 极端情况下出现文件丢失、覆盖、无法恢复或运行数据损坏的风险

## 2. 用户责任

使用者在操作前，应自行确认以下事项：

- 图片根目录设置正确
- 默认复制目标目录设置正确
- 当前操作对象确实是想要处理的文件
- 重要文件已经提前备份
- 相关目录具有必要的读写权限
- 磁盘空间充足
- 对“复制”“删除到应用内回收区”“清理回收区”的区别有明确理解

如果文件具有重要价值，建议先在少量测试文件或测试目录中验证流程。

## 3. 关于安全删除与应用内回收区

ZeroTraceBrowser 的删除操作默认是安全删除：文件会被移动到 ZeroTraceBrowser 的应用内回收区，而不是立即从磁盘上永久删除。

应用内回收区与 Windows 系统回收站不是同一个概念。

这意味着：

- 被删除的文件会保存在 ZeroTraceBrowser 管理的运行数据目录中
- 用户可以在 Recycle 页面尝试恢复文件
- 如果应用内回收区被清理，文件可能无法再通过 ZeroTraceBrowser 恢复
- 如果运行数据目录被手动删除、移动或损坏，恢复功能可能失效

在清理回收区前，请务必确认其中的文件不再需要。

## 4. 关于运行数据的说明

ZeroTraceBrowser 会在 `data/roots/<hash_id>/` 下为每个图片根目录保存独立的运行数据，包括但不限于：

- 缩略图缓存
- 图片索引
- 时间线索引
- 操作日志
- 应用内回收区文件
- 重复检测结果
- 任务输出

这些数据用于提升浏览速度、保存操作历史和隔离不同图片根目录的状态。

在 Settings 页面移除图片根目录并选择清理相关数据时，上述派生数据可能会被删除。该操作不会主动删除原始图片目录中的图片文件，但会影响缩略图、索引、日志、回收区文件、重复检测结果和任务历史等运行数据。

请不要在不了解影响的情况下手动删除或移动 `data/roots/<hash_id>/` 下的文件。

## 5. 关于时间识别与时间线的说明

ZeroTraceBrowser 的时间线排序和分组依赖后端生成的 `timeline_time` / `timeline_ts`。

时间来源遵循以下规则：

1. 优先读取图片 EXIF 时间
2. 如果无法读取 EXIF 时间，则使用文件时间作为 fallback

因此，时间线显示、排序、分组以及相关任务结果依赖于文件本身时间信息的准确性。

如果 EXIF 信息错误、缺失，或文件时间已经被系统、同步工具、复制工具、压缩软件或其他程序改变，最终显示的时间线可能与真实拍摄时间不一致。

## 6. 关于重复检测的说明

重复检测结果仅作为操作参考，不构成绝对判断。

- `strict` 模式仅将字节级完全一致的文件视为重复
- `phash` 模式基于视觉相似度判断，不能保证两个文件在业务、法律或归档意义上完全相同
- 图片尺寸、压缩方式、裁剪、滤镜、截图、水印、元数据变化等都可能影响检测结果
- 即使程序将文件标记为重复，涉及重要资料时仍应由使用者人工复核

ZeroTraceBrowser 不会因为重复检测结果自动删除文件。是否删除、复制或保留文件，应由用户自行判断并明确操作。

## 7. 关于日志、索引和缩略图的说明

ZeroTraceBrowser 会生成日志、图片索引、时间线索引和缩略图缓存，但这些数据仅用于辅助浏览、排查和记录。

这些数据不构成对以下内容的保证：

- 文件操作一定成功
- 操作记录一定完整
- 索引一定实时反映磁盘上的全部变化
- 缩略图一定与原图当前状态完全一致
- 已删除文件一定可以恢复

如果外部程序同时修改图片目录，或者用户手动移动、删除、重命名文件，ZeroTraceBrowser 中显示的状态可能需要刷新、重新扫描或重新生成索引后才会更新。

## 8. 关于第三方库与运行环境

本工具依赖 Python、FastAPI、Pillow、浏览器和操作系统文件系统等运行环境。

不同系统、磁盘、权限设置、文件格式、图片元数据、第三方库版本可能导致行为差异。

对于损坏文件、异常 EXIF、特殊格式图片、超大图片或权限受限文件，本工具可能无法正常读取、生成缩略图、显示元数据或执行文件操作。

## 9. 责任限制

本工具按“现状”提供，不附带任何明示或暗示担保。

开发者或提供者不对以下情况承担责任：

- 因使用或无法使用本工具造成的直接或间接损失
- 因误操作导致的文件错放、覆盖、删除或丢失
- 因用户未备份重要数据而产生的损失
- 因操作系统、权限、硬件、磁盘、第三方库或运行环境问题造成的异常
- 因时间识别、重复检测、索引缓存或缩略图缓存不准确造成的判断错误
- 因用户手动修改运行数据目录导致的不可恢复问题

## 10. 不构成专业或合规建议

本工具仅为本地图片浏览与文件操作辅助工具，不构成法律意见、合规意见、档案管理意见、取证建议或其他专业建议。

如果你的文件涉及法律留存、监管要求、证据材料、企业合规管理、医疗记录、财务记录或其他高风险用途，请在使用前自行完成额外评估、备份和验证。

## 11. 建议的安全使用方式

为降低风险，建议按以下方式使用本工具：

1. 首次使用时先使用测试目录或少量测试文件
2. 正式处理前备份重要图片和视频
3. 在 Settings 页面确认图片根目录和复制目标目录
4. 先执行少量复制或删除操作，确认结果符合预期
5. 删除文件后，在 Recycle 页面确认文件可以正常查看或恢复
6. 清理应用内回收区前，逐项确认文件不再需要
7. 处理重复图片时，先人工复核，再执行删除或批量操作
8. 不要手动删除或移动 `data/roots/<hash_id>/` 下的运行数据，除非你明确知道影响

## 12. 说明

当你运行或使用 ZeroTraceBrowser 时，视为你已理解并接受上述风险与限制。
