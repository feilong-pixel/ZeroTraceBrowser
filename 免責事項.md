# ZeroTraceBrowser 免責事項

## 1. 利用上のリスクについて

ZeroTraceBrowser は、ローカル画像の閲覧と、ユーザーが明示的に実行するファイル操作を補助するツールです。

本ツールは、ユーザーの代わりに自動で整理、削除、移動を行うものではありません。コピー、削除、一括処理、リサイクル領域のクリーンアップ、ディレクトリ履歴データの削除などの操作は、いずれもユーザーが明示的に実行する必要があります。

本ツールは、できるだけ予測可能で復元しやすい形でファイルを扱うよう設計されていますが、実際の利用では、ファイル権限、パス設定の誤り、ディスク状態、ファイルの使用中状態、予期しない中断、システム上の制限、第三者ライブラリの挙動、その他の予見できない要因により、以下のような事象が発生する可能性があります。

- ファイル操作の結果が期待どおりにならない
- ファイルのコピーに失敗する、または意図しない場所へコピーされる
- ファイルがアプリ内リサイクル領域へ移動された後、期待どおりに復元できない
- サムネイル、インデックス、ログ、タスク結果が不完全になる
- 重複検出結果が不正確、または不完全になる
- 極端な場合、ファイルの消失、上書き、復元不能、または実行時データの破損が発生する

## 2. 利用者の責任

操作を行う前に、利用者は以下を自ら確認する必要があります。

- 画像ルートディレクトリが正しく設定されていること
- デフォルトのコピー先ディレクトリが正しく設定されていること
- 現在選択している操作対象が、本当に処理したいファイルであること
- 重要なファイルを事前にバックアップしていること
- 関連ディレクトリに必要な読み書き権限があること
- 十分なディスク容量があること
- 「コピー」「アプリ内リサイクル領域への削除」「リサイクル領域のクリーンアップ」の違いを理解していること

重要なファイルを扱う場合は、少量のテストファイルまたはテスト用ディレクトリで事前に動作を確認することを推奨します。

## 3. 安全な削除とアプリ内リサイクル領域について

ZeroTraceBrowser の削除操作は、デフォルトでは安全な削除として扱われます。ファイルは即座にディスクから完全削除されるのではなく、ZeroTraceBrowser のアプリ内リサイクル領域へ移動されます。

アプリ内リサイクル領域は、Windows のシステムごみ箱とは別のものです。

つまり、次の点に注意が必要です。

- 削除されたファイルは、ZeroTraceBrowser が管理する実行時データディレクトリに保存されます
- Recycle ページから復元を試みることができます
- アプリ内リサイクル領域をクリーンアップした場合、ZeroTraceBrowser から復元できなくなる可能性があります
- 実行時データディレクトリが手動で削除、移動、または破損した場合、復元機能が利用できなくなる可能性があります

リサイクル領域をクリーンアップする前に、その中のファイルが不要であることを必ず確認してください。

## 4. 実行時データについて

ZeroTraceBrowser は、各画像ルートに対して `data/roots/<hash_id>/` 以下に独立した実行時データを保存します。これには、以下が含まれます。

- サムネイルキャッシュ
- 画像インデックス
- タイムラインインデックス
- 操作ログ
- アプリ内リサイクル領域のファイル
- 重複検出結果
- タスク出力

これらのデータは、閲覧速度の向上、操作履歴の保持、画像ルートごとの状態分離のために使用されます。

Settings ページで画像ルートを削除し、関連データのクリーンアップを選択した場合、上記の派生データが削除されることがあります。この操作は、元の画像ディレクトリ内の画像ファイルを自動的に削除するものではありませんが、サムネイル、インデックス、ログ、リサイクル領域のファイル、重複検出結果、タスク履歴などの実行時データに影響します。

影響を理解していない場合は、`data/roots/<hash_id>/` 以下のファイルを手動で削除または移動しないでください。

## 5. 時刻判定とタイムラインについて

ZeroTraceBrowser のタイムラインの並び替えとグルーピングは、バックエンドで生成された `timeline_time` / `timeline_ts` に依存します。

時刻の取得元は、次の順序で決定されます。

1. 画像の EXIF 時刻を優先して読み取る
2. EXIF 時刻を取得できない場合は、ファイル時刻を fallback として使用する

そのため、タイムライン表示、並び替え、グルーピング、および関連するタスク結果は、ファイル自体の時刻情報の正確性に依存します。

EXIF 情報が誤っている、欠落している、またはファイル時刻が OS、同期ツール、コピー処理、圧縮ソフト、その他のプログラムによって変更されている場合、表示されるタイムラインが実際の撮影時刻と一致しないことがあります。

## 6. 重複検出について

重複検出結果は、あくまで操作上の参考情報であり、絶対的な判断ではありません。

- `strict` モードは、バイト単位で完全に一致するファイルのみを重複として扱います
- `phash` モードは視覚的な類似度に基づくため、業務上、法的、またはアーカイブ上の意味で完全に同一であることを保証しません
- 画像サイズ、圧縮方式、トリミング、フィルタ、スクリーンショット、透かし、メタデータ変更などが検出結果に影響する場合があります
- 重要な資料については、プログラムが重複と表示した場合でも、利用者自身で確認してください

ZeroTraceBrowser は、重複検出結果に基づいて自動的にファイルを削除することはありません。削除、コピー、保持の判断は、利用者が自ら行い、明示的に操作する必要があります。

## 7. ログ、インデックス、サムネイルについて

ZeroTraceBrowser は、ログ、画像インデックス、タイムラインインデックス、サムネイルキャッシュを生成しますが、これらのデータは閲覧、確認、記録の補助を目的としたものです。

これらのデータは、以下を保証するものではありません。

- ファイル操作が必ず成功すること
- 操作記録が必ず完全であること
- インデックスがディスク上のすべての変更を常に即時反映すること
- サムネイルが現在の元画像の状態と完全に一致すること
- 削除済みファイルが必ず復元できること

外部プログラムが同時に画像ディレクトリを変更した場合、または利用者が手動でファイルを移動、削除、リネームした場合、ZeroTraceBrowser 上の表示状態は、更新、再スキャン、またはインデックス再生成を行うまで最新状態にならないことがあります。

## 8. 第三者ライブラリと実行環境について

本ツールは、Python、FastAPI、Pillow、ブラウザ、OS のファイルシステムなどの実行環境に依存しています。

OS、ディスク、権限設定、ファイル形式、画像メタデータ、第三者ライブラリのバージョンによって、挙動に差異が生じる場合があります。

破損ファイル、異常な EXIF、特殊な画像形式、非常に大きい画像、または権限が制限されたファイルについては、正常に読み取り、サムネイル生成、メタデータ表示、ファイル操作ができない場合があります。

## 9. 責任の制限

本ツールは「現状のまま」提供され、明示または黙示を問わず、いかなる保証も伴いません。

開発者または提供者は、以下について責任を負いません。

- 本ツールの使用または使用不能によって生じた直接的または間接的損害
- 誤操作によって生じたファイルの誤配置、上書き、削除、消失
- 利用者が重要データをバックアップしていなかったことによる損失
- OS、権限、ハードウェア、ディスク、第三者ライブラリ、実行環境に起因する不具合
- 時刻判定、重複検出、インデックスキャッシュ、サムネイルキャッシュの不正確さによる判断ミス
- 利用者が実行時データディレクトリを手動で変更したことによる復元不能な問題

## 10. 専門的またはコンプライアンス上の助言ではないこと

本ツールは、ローカル画像閲覧およびファイル操作を補助するツールであり、法的助言、コンプライアンス助言、記録管理助言、証拠保全上の助言、その他の専門的助言を提供するものではありません。

法的保存義務、規制対象記録、証拠資料、企業コンプライアンス、医療記録、財務記録、その他高リスク用途に関係するファイルを扱う場合は、利用前に別途評価、バックアップ、検証を行ってください。

## 11. 安全な利用方法の推奨

リスクを低減するため、以下の方法で利用することを推奨します。

1. 初回利用時は、テスト用ディレクトリまたは少量のテストファイルを使用する
2. 本格的な処理の前に、重要な画像や動画をバックアップする
3. Settings ページで画像ルートとコピー先ディレクトリを確認する
4. まず少量のコピーまたは削除操作を行い、結果が期待どおりであることを確認する
5. ファイル削除後、Recycle ページでファイルが正常に表示または復元できることを確認する
6. アプリ内リサイクル領域をクリーンアップする前に、各ファイルが不要であることを確認する
7. 重複画像を処理する場合は、先に目視確認し、その後に削除または一括操作を実行する
8. 影響を理解していない限り、`data/roots/<hash_id>/` 以下の実行時データを手動で削除または移動しない

## 12. 最終確認

ZeroTraceBrowser を実行または使用した時点で、利用者は上記のリスクおよび制限事項を理解し、承諾したものとみなされます。
