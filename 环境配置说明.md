# ZeroTraceBrowser 环境配置说明

## 1. 项目简介

ZeroTraceBrowser 是一个基于 Python + FastAPI 的本地图片浏览与受控文件操作工具。

项目通过本地 Web 服务提供界面，用于：

- 浏览本地图片目录
- 查看图片与基础元数据
- 生成缩略图缓存
- 按时间线排序和分组
- 复制图片到目标目录
- 将图片移动到应用内回收区
- 生成 Hash DB 和重复图片检测结果
- 管理应用内回收区和运行数据

本项目面向本地使用，不需要数据库、云服务或外部账号。

## 2. 推荐环境

- 操作系统：Windows 10 或 Windows 11
- Python：3.10 或更高版本
- 终端：PowerShell
- 浏览器：Microsoft Edge、Chrome、Firefox 等现代浏览器

推荐在项目根目录下使用 repo-local 虚拟环境：

```text
.\venv\
```

这样可以避免依赖安装到系统 Python 或其他项目的虚拟环境中。

## 3. Python 依赖

项目依赖由 `requirements.txt` 管理，当前包括：

- `fastapi`
- `uvicorn[standard]`
- `Pillow`
- `exifread`
- `pywin32`（Windows）
- `pytest`
- `httpx`

用途说明：

- `FastAPI`：后端 Web API
- `uvicorn`：本地 Web 服务运行器
- `Pillow`：图片读取、缩略图生成、部分元数据处理
- `exifread`：读取 EXIF 元数据
- `pywin32`：Windows 下保留文件时间等系统文件操作
- `pytest`：自动化测试
- `httpx`：测试客户端依赖

## 4. 创建虚拟环境

在项目根目录打开 PowerShell：

```powershell
cd D:\path\to\ZeroTraceBrowser
```

创建虚拟环境：

```powershell
python -m venv venv
```

创建完成后，项目目录下会出现：

```text
venv\
```

常用路径：

```text
.\venv\Scripts\python.exe
.\venv\Scripts\pip.exe
```

如果想激活虚拟环境，可以执行：

```powershell
.\venv\Scripts\Activate.ps1
```

如果 PowerShell 阻止脚本执行，可在当前终端会话中临时允许：

```powershell
Set-ExecutionPolicy -Scope Process RemoteSigned
```

不过，本项目推荐直接使用 `.\venv\Scripts\python.exe`，即使不激活虚拟环境也能明确使用项目自己的 Python。

## 5. 安装依赖

推荐使用明确路径安装：

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

可选：升级 pip 后再安装：

```powershell
.\venv\Scripts\python.exe -m pip install --upgrade pip
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

验证依赖是否可用：

```powershell
.\venv\Scripts\python.exe -c "import fastapi, uvicorn; from PIL import Image; print('Environment OK')"
```

如果需要单独确认 EXIF 和 Windows 文件操作依赖：

```powershell
.\venv\Scripts\python.exe -c "import exifread; print('exifread OK')"
.\venv\Scripts\python.exe -c "import win32file, win32con; print('pywin32 OK')"
```

`pywin32` 仅在 Windows 环境中需要；本项目推荐运行在 Windows 10 / Windows 11。

## 6. 启动服务

推荐使用项目脚本：

```powershell
.\start.ps1
```

该脚本会在项目根目录启动：

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```

也可以手动执行：

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```

启动成功后，终端通常会显示类似：

```text
Uvicorn running on http://127.0.0.1:8000
```

然后在浏览器中打开：

```text
http://127.0.0.1:8000
```

## 7. 端口说明

默认端口为：

```text
8000
```

如果端口已被占用，可以临时换一个端口手动启动，例如：

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8001
```

然后访问：

```text
http://127.0.0.1:8001
```

注意：`start.ps1` 当前固定使用 `8000` 端口。

## 8. 运行测试

推荐使用项目脚本：

```powershell
.\test.ps1
```

该脚本会执行：

```powershell
.\venv\Scripts\python.exe -m pytest -q
```

也可以直接运行：

```powershell
.\venv\Scripts\python.exe -m pytest -q
```

## 9. 项目主要文件和目录

```text
ZeroTraceBrowser/
├── app.py                      # FastAPI 应用入口
├── requirements.txt            # Python 依赖
├── requirements-dev.txt        # 测试依赖
├── start.ps1                   # 启动脚本
├── test.ps1                    # 测试脚本
├── pyproject.toml              # 项目元数据

├── core/                       # 后端核心
│   ├── app/                    # 应用工厂、生命周期、中间件
│   ├── config/                 # 路径、扩展名、支持格式
│   ├── context_modules/        # 上下文模块（按领域拆分）
│   ├── domain/                 # 领域模型：RootContext、ImageEntry 等
│   ├── infrastructure/         # 文件传输、哈希计算、图片处理
│   ├── repositories/           # 数据访问层
│   ├── routes/                 # API 路由
│   ├── services/               # 业务服务
│   └── use_cases/              # Use-case 层（复制、删除、恢复等）
├── MediaArchiveOrganizer/      # 图片分析与整理相关模块
├── tests/                      # 自动化测试
├── data/                       # 运行数据（按图片根目录隔离）
│   └── roots/<root_id>/
│       ├── root.json           # 根目录配置
│       ├── deleted/            # 应用内回收区
│       ├── thumbnails/         # 缩略图缓存
│       ├── logs/               # 操作日志（CSV 格式）
│       ├── indexes/            # 图片索引 / 时间线索引
│       ├── tasks/              # 任务输出（按任务隔离）
│       ├── hash_db.sqlite3     # 内容哈希数据库
│       └── duplicates.json     # 重复图片检测结果
```

当前主要运行数据按图片根目录隔离在：

```text
data/roots/<root_id>/
```

其中可能包含：

- `deleted/`：应用内回收区
- `thumbnails/`：缩略图缓存
- `logs/`：操作日志
- `indexes/`：图片索引和时间线索引
- `tasks/`：任务输出
- `duplicates.json`：重复图片结果
- `hash_db.sqlite3`：Hash DB

## 10. 本地设置文件

项目设置保存在：

```text
settings.json
```

常见设置包括：

- 当前图片根目录
- 已添加的图片根目录列表
- 默认复制目标目录
- 语言设置
- 任务页面默认值
- 每个图片根目录的统计摘要

建议优先通过 Settings 页面修改设置，不要在不了解格式的情况下手动编辑 `settings.json`。

## 11. 常见环境问题

### Python 无法识别

如果 PowerShell 提示找不到 `python`，请确认：

- 已安装 Python
- Python 已加入系统 `PATH`
- 或使用 Python Launcher：`py -m venv venv`

### 依赖安装到了错误环境

建议始终使用：

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

不要只依赖 `pip install ...`，因为它可能指向系统 Python 或其他环境。

可以检查当前 Python：

```powershell
.\venv\Scripts\python.exe -c "import sys; print(sys.executable)"
```

### Pillow / exifread / pywin32 未安装或导入失败

如果出现 `PIL`、`exifread`、`win32file` 或 `win32con` 相关错误，请重新安装依赖：

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

也可以分别检查包是否安装在当前虚拟环境中：

```powershell
.\venv\Scripts\python.exe -m pip show Pillow
.\venv\Scripts\python.exe -m pip show exifread
.\venv\Scripts\python.exe -m pip show pywin32
```

### PowerShell 无法执行脚本

如果 `Activate.ps1`、`start.ps1` 或 `test.ps1` 被执行策略阻止，可在当前终端会话中执行：

```powershell
Set-ExecutionPolicy -Scope Process RemoteSigned
```

然后重新运行脚本。

### 端口 8000 被占用

如果启动失败并提示端口被占用，可以：

1. 关闭已运行的 ZeroTraceBrowser 服务
2. 或手动使用其他端口启动：

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8001
```

### 页面看起来没有更新

如果修改了前端 JS、CSS 或后端代码，但浏览器仍显示旧行为：

1. 重启 Uvicorn 服务
2. 刷新浏览器页面
3. 必要时使用浏览器强制刷新

后端代码修改通常需要重启服务才会生效。

### 图片无法显示或缩略图加载慢

首次打开大型图片目录时，缩略图可能需要生成，初次加载会比较慢。后续访问通常会命中缓存。

如果图片无法显示，请确认：

- 图片根目录存在
- 当前用户具有读取权限
- 文件格式受支持
- 服务端终端是否有错误日志

## 12. 推荐首次配置流程

1. 在项目根目录打开 PowerShell
2. 创建虚拟环境：

   ```powershell
   python -m venv venv
   ```

3. 安装依赖：

   ```powershell
   .\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
   ```

4. 启动服务：

   ```powershell
   .\start.ps1
   ```

5. 打开浏览器：

   ```text
   http://127.0.0.1:8000
   ```

6. 进入 Settings 页面，添加图片根目录
7. 按需设置默认复制目标目录
8. 返回 Gallery 页面开始浏览
9. 如需验证项目状态，运行：

   ```powershell
   .\test.ps1
   ```

## 13. 安全建议

- 当前版本建议仅在本机或可信局域网环境中使用
- 不建议将服务直接暴露到公网
- 对重要图片目录执行批量操作前，请先备份
- 删除文件后，请在 Recycle 页面确认是否可以恢复
- 清理应用内回收区前，请确认文件不再需要
- 不要随意删除 `data/roots/<root_id>/` 下的运行数据

## 14. 快速命令汇总

```powershell
python -m venv venv
.\venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
.\start.ps1
```

测试：

```powershell
.\test.ps1
```

手动启动：

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
```
